@model IEnumerable<TOURTRADE.SysLoginTT.EN.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Descripcion
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreCategoria)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DescripcionCategoria)
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { Id = item.CategoriaID })" class="btn btn-success"> <i class="fas fa-edit"></i></a>
                    <a href="@Url.Action("Detail", new { Id = item.CategoriaID })" class="btn btn-info"> <i class="fas fa-eye"></i></a>
                    <a href="@Url.Action("Delete", new { Id = item.CategoriaID })" class="btn btn-danger"> <i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>